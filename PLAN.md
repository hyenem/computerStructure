# 컴퓨터 구조 학습 웹페이지 — 계획서

> 컴퓨터 **본체**에서 시작해, 모듈을 누를 때마다 그 내부로 **점점 딥다이브**하며
> 컴퓨터 구조를 직관적으로 배우는 인터랙티브 학습 사이트.

---

## 1. 컨셉 (Core Idea)

**"줌인(Zoom-in) 드릴다운 탐험"**

사용자는 컴퓨터 본체 그림에서 출발한다. 궁금한 부품을 클릭하면 화면이 그 부품
**안으로 들어가며**(확대/전환), 더 작은 구성요소들이 나타난다. 이 과정을 반복하며
"케이스 → 부품 → 칩 → 회로 → 논리게이트 → 트랜지스터 → 반도체 → 원자" 까지 내려간다.

- **물리적 줌인 무드**: 단순한 도식 전환이 아니라 *실제로 점점 작아지는* 느낌.
  매 단계마다 **크기 스케일 표시**(예: 약 40cm → 4cm → 14nm → 0.2nm)를 보여줘
  "지금 얼마나 깊이 들어왔는지"를 체감하게 한다. (스케일 바 / 줌 게이지)
- **대상**: 전공 입문자. 2진수·불 대수·페치-디코드-실행 같은 핵심 개념을
  부담 없이 처음 접하도록, 그러나 *얼버무리지 않고* 끝까지 내려간다.
- 항상 **현재 어디에 있는지**(빵부스러기 경로)를 보여준다.
- 각 단계마다 **"이게 뭐고, 무슨 일을 하는가"** 를 설명하고, 가능한 곳엔 **작은 실습**을 둔다.
- 뒤로(상위로) 자유롭게 나갈 수 있다.

---

## 2. 탐험 트리 (Drill-down Map)

콘텐츠의 뼈대. 각 노드는 `제목 + 한줄설명 + 자세한설명 + 하위노드들`로 구성된다.

각 노드 우측의 `📏`는 대략적 **크기 스케일**(물리적 줌인 게이지용), `🧪`는 **실습** 후보.

```
L0  💻 컴퓨터 본체 (Case / Tower)               📏 ~40cm
│
├─ L1  메인보드 (Motherboard)                   📏 ~30cm
│   ├─ L2  CPU (중앙처리장치)                    📏 ~4cm   🧪 페치-디코드-실행 1스텝씩
│   │   ├─ L3  제어장치 (Control Unit)
│   │   ├─ L3  연산장치 (ALU)                    📏 ~mm    🧪 연산 선택→결과 보기
│   │   │   ├─ L4  가산기 (Adder)                          🧪 반/전가산기 입력 토글
│   │   │   │   └─ L5  논리게이트 (AND/OR/NOT/XOR) 📏 ~µm   🧪 입력 0/1 토글→출력
│   │   │   │       └─ L6  트랜지스터 (MOSFET)    📏 ~14nm  🧪 게이트 전압→ON/OFF 스위치
│   │   │   │           └─ L7  반도체 (PN접합·도핑)📏 ~nm
│   │   │   │               └─ L8  실리콘 결정/원자 📏 ~0.2nm 🧪 전자 흐름 = 전류
│   │   │   └─ L4  논리연산 회로 (불 대수)                  🧪 진리표 만들어보기
│   │   ├─ L3  레지스터 (Register)                         🧪 플립플롭: 클럭으로 값 저장
│   │   └─ L3  캐시 메모리 (L1/L2/L3)
│   ├─ L2  RAM (주기억장치)                      📏 ~10cm  🧪 주소로 읽기/쓰기
│   │   └─ L3  메모리 셀 → 주소·데이터 구조
│   ├─ L2  칩셋 / 버스 (Bus)
│   └─ L2  ROM / BIOS·UEFI
│
├─ L1  GPU (그래픽카드)                          📏 ~25cm
│   └─ L2  코어 / VRAM
│
├─ L1  저장장치 (Storage)
│   ├─ L2  SSD → 플래시 셀
│   └─ L2  HDD → 플래터·헤드
│
├─ L1  전원공급장치 (PSU)
├─ L1  냉각 시스템 (Cooler / Fan)
└─ L1  입출력 포트 (I/O Ports)
```

> **굵은 경로** = 본체 → 메인보드 → CPU → ALU → 가산기 → 논리게이트 → 트랜지스터 →
> 반도체 → 원자. 1차 목표는 이 한 줄기를 **끝까지(L8)** 완성해 "물리적 줌인" 데모를
> 확보하는 것. 나머지 가지(GPU·저장장치 등)는 이후 확장.

### 2-1. 실습(미니랩) 목록 — 우선순위
콘텐츠 외에 별도 엔지니어링이 필요한 인터랙티브 요소. ⭐=먼저 만들 후보.
- ⭐ **논리게이트 토글** (L5): 입력 0/1 클릭 → 출력 실시간 + 진리표 하이라이트
- ⭐ **반/전가산기** (L4): 두 비트 입력 → 합·자리올림, 내부 게이트 점등
- ⭐ **트랜지스터 스위치** (L6): 게이트 전압 슬라이더 → 전류 ON/OFF 시각화
- **레지스터/플립플롭** (L3): 클럭 누를 때 값 래치
- **2진수 ↔ 10진수 변환기** (보조 위젯, 여러 곳에서 재사용)
- **페치-디코드-실행** (L2): 아주 단순한 가상 CPU 1명령 실행 애니메이션 (난이도 높음 → 후순위)

---

## 3. 기술 스택 (Tech Stack)

심플하고 git 배포가 쉬운 조합을 우선한다.

| 항목 | 선택 | 이유 |
|------|------|------|
| 형태 | 정적 SPA (빌드 불필요) | GitHub Pages에 그대로 올리면 끝 |
| 언어 | HTML + CSS + 순수 JavaScript | 의존성 없이 가볍고 빠르게 시작 |
| 데이터 | `data.js` 안의 JS 객체(트리) | 콘텐츠와 화면 로직 분리 |
| 그래픽 | SVG / CSS (필요시 이미지) | 부품을 클릭 가능한 영역으로, 확대 애니메이션 |
| 배포 | GitHub Pages | 무료, git push로 자동 반영 |

> 대안: 규모가 커지면 Vite + React로 확장 가능. **1단계는 순수 JS로 시작** 권장.

---

## 4. 화면 구성 (UI Layout)

**선택: 스테이지 + 사이드패널** (왼쪽 깊이 게이지 / 가운데 큰 스테이지 / 오른쪽 설명·미니랩)

```
┌─본체 › 메인보드 › CPU › ALU ──────────────────┐  ← 빵부스러기(클릭=상위 점프)
│░░░│                              │             │
│40 │                              │  ALU        │
│cm │     [    ALU 도식    ]       │  산술·논리 연산 │ ← 제목·한줄설명
│ │ │   ┌────┐ ┌────┐ ┌────┐     │  ─────────  │
│ ↓ │   가산기  논리   시프트 →    │  자세한 설명  │
│ ↑ │   └────┘ └────┘ └────┘     │  ─────────  │
│ │ │   (하위=클릭 핫스팟, 줌인)    │  🧪 미니랩    │ ← 설명 바로 아래
│0.2│                              │  [A]0 [B]1   │   인라인 실습
│nm │                              │   → 출력 1   │
├───┴──────────────────────────────┴─────────────┤
│ [← 뒤로]                  [⚙ 설정]   [처음으로 🏠]│
└────────────────────────────────────────────────┘
```

- **왼쪽 — 깊이 게이지**: 세로 눈금(40cm↔0.2nm). 현재 깊이를 마커로 표시 → *물리적 줌인 무드의 핵심*. 클릭/드래그로 깊이 점프도 가능(추후).
- **가운데 — 스테이지**: 현재 모듈 도식 + 하위 부품을 **클릭 가능한 핫스팟**으로. 클릭 시 그 부품이 **화면을 꽉 채우게 확대된 뒤** 내부 도식이 드러남(중간 강도 줌인).
- **오른쪽 — 설명 패널**: 이름·한줄설명·자세한 설명, 그리고 **바로 아래 인라인 미니랩**(⭐ 게이트·가산기·트랜지스터부터).
- **상단 — 빵부스러기** / **하단 — 뒤로·설정·처음으로**.
- **반응형**: 모바일에선 세로로 스택(스테이지 위 / 설명·미니랩 아래), 깊이 게이지는 상단 가로 바로 전환.

---

## 5. 파일 구조

```
computerStructure/
├─ index.html        # 뼈대
├─ css/style.css     # 디자인·애니메이션
├─ js/
│   ├─ data.js       # 탐험 트리(콘텐츠) — 핵심
│   └─ app.js        # 렌더링·네비게이션 로직
├─ assets/           # SVG/이미지
├─ PLAN.md           # (이 문서)
└─ README.md         # 소개·실행법
```

---

## 6. 개발 단계 (Milestones)

- **M1 — 뼈대**: index.html + 2분할 레이아웃 + 빵부스러기 + **스케일 게이지** 자리.
- **M2 — 데이터 모델**: `data.js`에 굵은 경로(L0~L8) 트리 정의 + 각 노드 `scale` 필드.
- **M3 — 네비게이션**: 노드 클릭 → 하위로 줌인 / 뒤로 / 처음으로 + 스케일 표시 갱신.
- **M4 — 물리적 줌인 연출**: SVG 부품 + 클릭 핫스팟 + **확대해서 안으로 들어가는** 전환.
- **M5 — 미니랩 (실습)**: §2-1 ⭐ 항목(논리게이트·가산기·트랜지스터) 인터랙티브 구현.
- **M6 — 콘텐츠 채우기**: 설명 텍스트 보강, 나머지 가지(GPU·저장장치 등) 확장.
- **M7 — 디자인·설정**: 반응형(모바일), 색·타이포, 접근성, 설정 패널(표현모드/언어 슬롯).
- **M8 — 배포**: GitHub 저장소 생성 → Pages 활성화 → 공개 URL.

---

## 7. 배포 (Deploy)

1. `git init` → 첫 커밋.
2. GitHub에 저장소 생성 후 push.
3. Settings → Pages → 브랜치 `main` / 루트(`/`) 선택.
4. `https://<사용자명>.github.io/<저장소명>/` 으로 공개.

> 정적 사이트라 빌드 단계 없이 push만으로 배포된다.

---

## 8. 결정 사항 (Decisions) ✅

1. **스택**: 순수 JS로 시작 (빌드 없음, GitHub Pages 직배포).
2. **시각 표현**: **SVG 도식으로 시작**. 단, `설정(Settings)`에서 표현 모드를
   전환할 수 있게 구조화 → 추후 `사진/일러스트`, `혼합` 모드 추가.
3. **언어**: **한국어 전용으로 시작**. 단, 데이터에 `en` 필드를 함께 보관해
   추후 `설정`에서 언어(한국어 / 한·영 병기) 전환 가능하게 준비.
4. **깊이 우선순위**: **굵은 경로(CPU 라인) 먼저** 완성 → 데모 동작 확보 후 확장.
5. **대상**: **전공 입문용** — 핵심 개념(2진수·불 대수·페치-디코드-실행)을 처음 접하는 톤.
6. **무드**: **물리적 줌인** — 크기 스케일 게이지로 "점점 작아지는" 체감을 핵심 연출로.
7. **깊이**: **가능한 깊게** — 트랜지스터에서 멈추지 않고 반도체(PN접합)·실리콘 원자까지.
8. **실습**: **작은 미니랩 포함** — §2-1 ⭐ 항목(게이트·가산기·트랜지스터)부터.

### 8-1. 설정(Settings) 패널 — 추후 확장 슬롯
- 🎨 **표현 모드**: 도식(기본) / 사진 / 혼합
- 🌐 **언어**: 한국어(기본) / 한·영 병기
- (지금은 UI만 두고 도식·한국어로 동작, 나머지는 점진 추가)

---

## 9. 다음 액션

**M1(뼈대) + M2(굵은 경로 데이터)** 구현 → 로컬에서 동작 확인 → 이후 확장·배포.
```
```

---

## 10. 시리즈 큰 그림 — "속으로(Into)" 3부작

같은 엔진·같은 학습 구조(드릴다운+게이지+미니랩+교재+퀴즈)로 3개 사이트를 만든다.

| | 1편 컴퓨터구조 (이 사이트) | 2편 운영체제 | 3편 네트워크 |
|---|---|---|---|
| 이름(안) | 기계 속으로 | 커널 속으로 | 회선 속으로 |
| 줌 은유 | 물리적 크기 40cm→0.2nm | 추상화/권한 레벨 (앱→커널→HW 경계) | 거리/계층 (클릭→지구 반대편) |
| 루트 | 본체 | 실행 중인 프로세스 | 주소창의 URL |
| 종착 | 원자/전자 | 하드웨어 경계 (=1편 입구) | 전선 위의 비트 (=1편 NIC) |

### 경계 원칙 (주제 소유권)
- **인터럽트**: 1편=IRQ 신호·하드웨어 / 2편=핸들러·컨텍스트 스위칭
- **가상 메모리**: 1편=MMU·TLB / 2편=교체 정책·스왑 운영
- **패킷**: 1편=NIC·캡슐화 입구 / 3편=TCP·라우팅·DNS 전체
- **시스템 콜·파일시스템·스케줄러·프로세스**: 2편
- 1편의 경계 노드에는 🚪 **"다음 편 예고" 다리 배지**를 달아 시리즈로 잇는다.

### 엔진 재사용
- 엔진: index.html + css + app.js (네비·게이지·지도·퀴즈 렌더러)
- 콘텐츠팩: data.js / scenes.js / labs.js / extras.js / book.js
- 새 시리즈 = 콘텐츠팩 교체. 게이지 라벨·축만 설정화 필요.
